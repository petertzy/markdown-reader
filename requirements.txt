markdown>=3.5.0
markdown2>=2.4.0
# Note: tkinterdnd2 has known issues on Apple Silicon Macs (M1/M2/M3/M4)
# Drag-and-drop will not work, but all other features remain functional
# Use "File → Open File" menu instead
tkinterdnd2>=0.3.0
py2app>=0.28
watchdog
html2text>=2020.1.16
python-docx>=1.1.0
pygments
ttkbootstrap
pypdf>=4.0.0
PyMuPDF>=1.23.0
weasyprint>=62.0
requests>=2.28.0
docling

# Optional: Advanced PDF conversion with Docling
# Install with: pip install docling
# Docling provides ML-based PDF understanding for better quality conversion
# of complex documents (tables, multi-column layouts, equations, etc.)
# docling